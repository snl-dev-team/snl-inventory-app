name: Deploy Product Lambda
on:
  push:
    paths:
      - "server/product.py"
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        function-name:
          [
            createProduct,
            fetchProducts,
            updateProduct,
            deleteProduct,
            productUseMaterial,
          ]

    steps:
      - uses: actions/checkout@master

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          cd server
          mkdir build
          pip install -r requirements.txt -t ./build

      - name: zip
        run: |
          cd server
          mv ./product.py ./build/product.py
          mv ./db_client.py ./build/db_client.py
          cd build
          zip -r ./../build.zip *

      - name: deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: ${{ matrix.function-name }}
          zip_file: server/build.zip
          runtime: python3.8
          handler: product.${{ matrix.function-name }}
          role: arn:aws:iam::595723023717:role/snl-inventory-app-lambda
